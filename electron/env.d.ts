/** Injected by vite's `define` at build time — see `vite.config.ts` and the
 * `OFFICIAL_BUILD_KEY` gate in `main.ts`. Empty string in any build that
 * didn't have `OFFICIAL_BUILD_KEY` set (a plain, un-configured clone). */
declare const __BUILD_KEY__: string
